/**
 * Library Configurations Barrel Export
 * 
 * Export all library configurations and wrappers from this file.
 * 
 * Example usage:
 * import { apiClient, queryClient } from '@/lib';
 */

export { apiClient } from './axios';
export { queryClient } from './react-query';
// TODO: Export additional library configurations as they are created
// export { firebaseApp } from './firebase';

