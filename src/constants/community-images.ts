/**
 * Community Images Constants
 * Centralized location for all default/fallback images used in community components
 */

export const COMMUNITY_IMAGES = {
  // Default image for posts without images
  DEFAULT_POST_IMAGE: '/ourbride-default-image.jpg',
  
  // Default image for articles without images
  DEFAULT_ARTICLE_IMAGE: '/ourbride-blog-default-image.jpg',
  
  // Default image for blogs without images
  DEFAULT_BLOG_IMAGE: '/ourbride-blog-default-image.jpg',
  
  // Default image for reels without thumbnails
  DEFAULT_REEL_IMAGE: '/ourbride-default-image.jpg',
  
  // Default image for contests without images
  DEFAULT_CONTEST_IMAGE: '/ourbride-default-image.jpg',
  
  // Default image for decision groups without images
  DEFAULT_DECISION_GROUP_IMAGE: '/ourbride-default-image.jpg',
  
  // Default avatar/author image (favicon)
  DEFAULT_AVATAR_IMAGE: '/favicon-32x32.png',
} as const




